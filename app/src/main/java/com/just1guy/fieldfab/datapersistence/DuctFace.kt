package com.just1guy.fieldfab.datapersistence

enum class DuctFace(val face: Int) {
    FRONT(0),
    BACK(1),
    LEFT(2),
    RIGHT(3);
}

enum class DuctFaceALL {
    FRONT, BACK, LEFT, RIGHT, ALL
}